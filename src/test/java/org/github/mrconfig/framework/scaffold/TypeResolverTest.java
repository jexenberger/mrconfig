package org.github.mrconfig.framework.scaffold;

import org.github.mrconfig.domain.KeyEntity;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

/**
 * Created by julian3 on 2014/07/20.
 */
public class TypeResolverTest {


    @Test
    public void testGetType() throws Exception {

        /*
        assertEquals("String", TypeResolver.getType(String.class));
        assertEquals("Number", TypeResolver.getType(Integer.class));
        assertEquals("Number", TypeResolver.getType(Double.class));
        assertEquals("Lookup", TypeResolver.getType(KeyEntity.class));
        */

    }
}
